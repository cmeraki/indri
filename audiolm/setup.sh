# this is a setup guide not a script yet
# 1. make sure you have cuda working : torch.cuda.devices() should return > 0
# 2. pip install encoded
# 3. create a data directory and put million hours of audio data in it
# 4. unsupervised pretraining requires just this much, for supervised get transcripts too
# 5.